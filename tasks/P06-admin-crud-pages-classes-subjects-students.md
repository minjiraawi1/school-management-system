Title: Frontend — Admin Pages (Classes, Subjects, Students)

Objective
- Build admin UI pages for core CRUD operations.

Prerequisites
- Frontend auth and routing in place.
- Backend CRUD endpoints for classes/subjects/students working.

Subtasks
- Manage Classes page: list, create, update, delete; forms and table with feedback.
- Manage Subjects page: list, create, update, delete; forms and table.
- Manage Students page: list, create, update, delete; forms and table.
- Integrate APIs; handle loading, error states, and success toasts.
- Use `shadcn/ui` components; Tailwind v4-compatible animation via `tw-animate-css`.
- Add basic validation on forms and graceful error messages.

Deliverables
- Three React components with routing links under admin dashboard.
- API service functions for each entity.

Acceptance Criteria
- Full CRUD works in UI; state updates reflect backend responses.
